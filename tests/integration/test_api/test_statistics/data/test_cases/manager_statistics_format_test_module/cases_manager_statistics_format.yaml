- name: remoted_endpoint
  description: remoted statistics endpoint
  configuration_parameters:
    ENDPOINT: fortishield-remoted
  metadata:
    endpoint: fortishield-remoted

- name: analysisd_endpoint
  description: analysisd statistics endpoint
  configuration_parameters:
    ENDPOINT: fortishield-analysisd
  metadata:
    endpoint: fortishield-analysisd

- name: fortishield-db_endpoint
  description: fortishield-db statistics endpoint
  configuration_parameters:
    ENDPOINT: fortishield-db
  metadata:
    endpoint: fortishield-db
